# PHASE 1 – STEP 1 — Venv + Docs Setup

> Shell: All commands in this step must be run in **Windows Command Prompt (CMD)**, not PowerShell.

**Project:** TravelAcross EU  
**Date:** 2025-11-24  
**Status:** ✅ Completed

---

## Purpose

- Create an isolated Python environment for the TravelAcross EU backend.
- Set up the documentation structure for step-based `.md` files.
- Record the exact commands used so we can recreate this setup anytime.

---

## Commands Executed (CMD on Windows)

From a normal CMD terminal:

```cmd
cd C:\projects\travelacrosseu
python --version
